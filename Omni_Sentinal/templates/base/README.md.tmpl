# Omni Sentinal (v7 Templates)

This install was generated by `Install_command_v7` template mode.

## Paths

- Base: `__BASE__`
- UI: `__UI__`

## Quick start

1. `cd __BASE__`
2. `docker compose up -d`
3. API health: <http://localhost:8000/health>
4. UI: <http://localhost:5173>

## Security

- Secrets are stored in `__BASE__/.env` with mode `600`.
- Use `Install_command_v7 --force` to overwrite existing generated files.
